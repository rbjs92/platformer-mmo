{
  "name": "platformer-mmo",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "npm install --prefix server && npm start --prefix server"
  },
  "engines": {
    "node": "10.15.2",
    "npm": "6.9.0"
  }
}

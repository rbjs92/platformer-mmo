import { Response } from 'express';
import { RequestInterface } from '../interfaces/RequestInterface';
declare const _default: {
    profile(req: RequestInterface, res: Response): Promise<void>;
};
export = _default;
